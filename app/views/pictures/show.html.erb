<p><%= @picture.title %>, <%= @picture.artist %></p>
<%= image_tag(@picture.url, :class => "artwork") %><br>

<%= link_to("Back to Gallery", "/pictures") %> |
<%= link_to("Edit", "/pictures/#{@picture.id}/edit") %> | <%=
link_to("Delete", "/pictures/#{@picture.id}", method: :delete, data: {confirm: "Are you sure you want to delete this picture?"}) %>

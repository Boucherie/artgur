<h1>21st Century Artists</h1>

<%= link_to("New Picture", "/pictures/new") %>

<% @pictures.each do |picture| %>
  <p><%= picture.title %>, <%= picture.artist %> </p>
  <%=  link_to(image_tag(picture.url, :class => "artwork"), "/pictures/#{picture.id}") %>

<% end %>
